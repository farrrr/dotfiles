# 根據 OS 忽略不適用的檔案
{{- if ne .chezmoi.os "darwin" }}
Library/
{{- end }}
{{- if ne .chezmoi.os "windows" }}
AppData/
Documents/
{{- end }}

# 不安裝 SSH 私鑰時忽略加密檔
{{- if not .install_ssh_keys }}
.ssh/id_ed25519_farrrr
.ssh/id_ed25519_github
.ssh/id_rsa_farrrr
{{- end }}

# Claude Code — 由 claude-brain 管理跨機器同步，chezmoi 不介入
.claude/
